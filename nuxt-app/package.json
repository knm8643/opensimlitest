{
  "name": "nuxt-app",
  "private": true,
  "homepage": "https://knm8643.github.io/opensimlitest/",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "push-lib": "git add -f dist && git commit -m 'Update library' && git push origin `git subtree split —prefix dist main`:gh-pages —force"
  },
  "devDependencies": {
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.4.3",
    "push-dir": "^0.4.1"
  }
}
