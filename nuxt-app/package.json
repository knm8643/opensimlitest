{
  "name": "nuxt-app",
  "private": true,
  "homepage": "https://knm8643.github.io/opensimlitest/",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "npm run build",
    "deploy": "nuxt generate && touch dist/.nojekyll && cd dist && git init && git add . && git commit -m 'deploy' && git push -f https://github.com/knm8643/opensimlitest.git main:gh-pages"
  },
  "devDependencies": {
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.4.3"
  }
}
